import { BrowserRouter, Route, Routes } from "react-router-dom";

const Rout = () => {
    return (
        <BrowserRouter>
            <Routes>
                <Route>
                    
                </Route>
            </Routes>
        </BrowserRouter>
    );
}